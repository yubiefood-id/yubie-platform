"use client";
import Link from "next/link";
import { useCart } from "@/features/cart/cart-context";
import { formatRupiah } from "@/lib/format";
export default function CheckoutPage() { const { items, subtotal } = useCart(); return <main id="main" className="checkout-page"><header><span className="eyebrow">CHECKOUT PROTOTYPE</span><h1>Almost there.</h1><p>Pembayaran belum terhubung. Tidak ada transaksi yang akan diproses dari halaman ini.</p></header><div className="checkout-grid"><form onSubmit={(event) => event.preventDefault()}><fieldset><legend>Contact</legend><label>Email<input type="email" /></label><label>WhatsApp<input inputMode="tel" /></label></fieldset><fieldset><legend>Delivery</legend><label>Nama lengkap<input /></label><label>Alamat<textarea rows={4} /></label><label>Kota<input /></label><label>Kode pos<input inputMode="numeric" /></label></fieldset><button className="button primary full" disabled>Payment integration coming soon</button></form><aside><h2>Order summary</h2>{items.map((item) => <div key={item.id}><span>{item.name} · {item.sizeLabel} × {item.quantity}</span><strong>{formatRupiah(item.unitPrice * item.quantity)}</strong></div>)}<div className="total"><span>Subtotal</span><strong>{formatRupiah(subtotal)}</strong></div>{!items.length && <p>Keranjang kosong. <Link href="/shop">Kembali ke shop.</Link></p>}</aside></div></main>; }
